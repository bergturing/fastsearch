#datasource配置
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/fast_search
    username: root
    password: 123546

#mybatis配置
mybatis:
  config-location: classpath:mybatis-config.xml